name: moneyseen_domain
description: Core MoneySeen entities, value objects, and domain rules.
version: 0.1.0
repository: https://github.com/suyie001/MoneySeen
publish_to: none

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies: {freezed_annotation: ^3.1.0, json_annotation: ^4.12.0}

dev_dependencies:
  build_runner: ^2.16.0
  freezed: ^4.0.0
  json_serializable: ^6.14.1
  lints: ^6.0.0
  test: ^1.25.6

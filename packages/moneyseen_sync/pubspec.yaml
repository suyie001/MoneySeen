name: moneyseen_sync
description: MoneySeen backup archive and sync provider contracts.
version: 0.1.0
repository: https://github.com/suyie001/MoneySeen
publish_to: none

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies:
  moneyseen_domain:
    path: ../moneyseen_domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
